engine: harmonics
version: "1.0"

requirements:
  base_window: 64
  min_window: 32
  max_window: 512
  scaling: linear

outputs:
  - n_harmonics
  - harmonic_ratio
  - harmonic_power

dependencies: []

metadata:
  description: Harmonic structure analysis
  category: frequency
