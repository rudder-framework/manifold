engine: variance_growth
version: "1.0"

requirements:
  base_window: 32
  min_window: 16
  max_window: 256
  scaling: linear

outputs:
  - variance_growth_rate
  - variance_growth_r2

dependencies: []

metadata:
  description: Non-stationarity via variance growth
  category: stationarity
