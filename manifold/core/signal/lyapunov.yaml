engine: lyapunov
version: "1.0"

requirements:
  base_window: 128
  min_window: 64
  max_window: 512
  scaling: linear

outputs:
  - lyapunov
  - embedding_dim
  - embedding_tau
  - confidence

dependencies: []

metadata:
  description: Lyapunov exponent for chaos detection
  category: dynamics
