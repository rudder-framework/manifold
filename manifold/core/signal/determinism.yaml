engine: determinism
version: "1.0"

requirements:
  base_window: 64
  min_window: 30
  max_window: 256
  scaling: linear

outputs:
  - determinism

dependencies: []

metadata:
  description: Determinism from RQA diagonal lines
  category: dynamics
